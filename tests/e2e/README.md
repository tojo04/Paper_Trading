# End-to-end tests

The Playwright journey is intentionally introduced after the order lifecycle exists. This folder
reserves its stable workspace location; Phase 14 will add the fake-market browser suite described in
`BUILD_STEPS.md`.
